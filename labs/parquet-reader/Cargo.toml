[package]
name = "parquet-reader"
version = "0.1.0"
edition = "2021"
description = "forge lab 04 — a footer parser and column-chunk reader over KSPQ, a Parquet-shaped teaching format: projection reads the minimum, malformed input is refused (columnspaces)"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kslab = { path = "../kit" }

[profile.release]
opt-level = 2
debug = false
