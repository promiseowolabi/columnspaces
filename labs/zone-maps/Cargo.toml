[package]
name = "zone-maps"
version = "0.1.0"
edition = "2021"
description = "forge lab 02 — block statistics and a pruning planner that never skips a block holding a match (columnspaces)"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kslab = { path = "../kit" }

[profile.release]
opt-level = 2
debug = false
