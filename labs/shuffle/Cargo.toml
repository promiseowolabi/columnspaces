[package]
name = "shuffle"
version = "0.1.0"
edition = "2021"
description = "forge lab 06 — a partitioned hash join with skew handling, and the broadcast-vs-partition decision, graded in bytes moved (columnspaces)"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kslab = { path = "../kit" }

[profile.release]
opt-level = 2
debug = false
