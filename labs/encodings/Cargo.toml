[package]
name = "encodings"
version = "0.1.0"
edition = "2021"
description = "forge lab 01 — four columnar codecs that round-trip byte-exact and never expand past their header bound (columnspaces)"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kslab = { path = "../kit" }

[profile.release]
opt-level = 2
debug = false
